T=d ld mf
V=x,y,z
R=-2, 2, .15

# This function is too complex for "float" accuracy

F=x*3+x*y+x*z+x*sin(y*z) - \
  (sin(x)+cos(y))*4 + \
  (sin(x)+cos(y))*x + \
  (sin(x)+cos(y))*y + \
  (sin(x)+cos(y))*z
